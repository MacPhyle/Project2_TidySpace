<!DOCTYPE html>
<html lang="en">
  <%- include("partials/head.ejs"); %>

  <body>
    <h1>Here's This Thing</h1>

    <ul class="itemListing">
      Item Name: <%= item.itemName.id %>
      <br>
      Purchase Price: <%= item.purchasePrice
      %>
      <br>
      Is It Broken? <%= item.broken ? "Yes" : "No" %>
      <br>
      Is It the Wrong Size?
      <%= item.wrongSize ? "Yes" : "No" %>
      <br>
      What Year Was It Last Used?
      <%= item.yearLastUsed %>
      <br>
      Tags: <%= item.tags.join(", ") %>
      <br>
      Notes: <%= item.itemNotes %>
    </ul>

    <button type="submit">
      <a href="/edit/<%= item %>">Click Here to Edit</a>
    </button>
  </body>

  <%- include("partials/footer.ejs"); %>
</html>
